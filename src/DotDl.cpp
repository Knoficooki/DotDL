
#include "DotDl.h"

using namespace std;

int main() {
    log(date());
    exeLog(execOut[0]);
    start();
    if (start() == 0){
        exeLog(execOut[1] + "\n\t\tis this the end?")
        return 0;
    }
    return 0;
}